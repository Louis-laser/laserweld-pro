# LaserWeld Pro · 软件更新及上线操作指南

> 合并 DEPLOY.md + 部署更新指南.md，覆盖全生命周期。
> 最后更新：2026-05-25

---

## 一、更新流程总览

```
修改代码/数据 → 本地测试 → Git提交 → Push到GitHub
                                       ├── EdgeOne 自动部署（国内主力）
                                       └── Vercel 手动部署（海外备用）
```

## 二、本地测试

```bash
# 启动本地服务器
cd ~/.openclaw/workspace/projects/激光焊接参数查询工具
python3 -m http.server 8080

# 浏览器打开 http://localhost:8080
```

本地测试要点：
- 改参数后选对应材料+厚度，看输出是否合理
- 改UI后检查移动端适配（F12 切换到手机视图）
- 改订阅逻辑后走一遍支付流程

---

## 三、部署（上线）

### 3.1 EdgeOne（国内主力，自动部署）

**前提条件：** GitHub 仓库已关联 EdgeOne Pages。

```bash
# 一条龙提交部署
cd ~/.openclaw/workspace/projects/激光焊接参数查询工具
git add .
git commit -m "更新说明"
git push
# EdgeOne 自动检测变更并部署（约 1-2 分钟）
```

**控制台：** https://console.cloud.tencent.com/edgeone/pages  
**项目名：** `laserweld-pro`  
**访问链接：** https://laserweld-pro-rkmikgiv.edgeone.cool

### 3.2 Vercel（海外备用，手动部署）

```bash
# 提交到 GitHub 后，执行：
cd ~/.openclaw/workspace/projects/激光焊接参数查询工具
npx vercel --prod --yes
```

**控制台：** https://vercel.com/luxiangzhi33-4259s-projects/laserweld-pro  
**访问链接：** https://laserweld-pro.vercel.app

### 3.3 部署策略对比

| 平台 | 部署方式 | 速度 | 国内访问 | 海外访问 | 回滚方式 |
|------|---------|------|---------|---------|---------|
| EdgeOne | git push 自动触发 | ~1min | ✅ 优 | ❌ 一般 | `git revert HEAD && git push` |
| Vercel | `npx vercel --prod --yes` | ~30s | ❌ 一般 | ✅ 优 | `npx vercel rollback` |

---

## 四、各类更新操作速查

### 4.1 修改焊接参数（最常见）

```bash
code data/params.js          # 1. 改数据
python3 -m http.server 8080  # 2. 本地测试
git add data/params.js       # 3. 提交
git commit -m "data: 修正XX参数"
git push                     # 4. 部署
```

### 4.2 修改界面/功能

```bash
code index.html              # 1. 改代码
# 本地测试...
git add index.html sw.js manifest.json  # 2. 提交
git commit -m "feat: XXX功能"
git push                     # 3. 部署
```

⚠️ 改动 index.html 或 sw.js 后，用户可能需要 Ctrl+F5 清缓存才能看到变化。

### 4.3 更换收款二维码

```bash
cp /path/to/new-wechat-qr.png assets/qr-wechat.png
cp /path/to/new-alipay-qr.png  assets/qr-alipay.png
git add assets/
git commit -m "fix: 替换收款二维码"
git push
```

⚠️ **无需改代码**，文件名保持不变即可。

### 4.4 纯数据修正（不涉及代码）

```bash
cd ~/.openclaw/workspace/projects/激光焊接参数查询工具
git add data/
git commit -m "fix: 修正XX材料焊接参数"
git push
```

### 4.5 添加新厚度段

1. 在 `index.html` 的 `ALL_THICKNESS_KEYS` 数组中添加新厚度值
2. 在 `data/params.js` 中添加对应厚度的参数数据
3. `findClosestThicknessKey()` 会自动匹配最近值

### 4.6 增加新的材料类型

1. 在 `index.html` 的材料网格 HTML 中添加新卡片
2. 在材料映射 `MATERIAL_KEYS` 中添加映射
3. 在 `data/params.js` 中添加新材料的数据
4. 如需评估 → 更新焊接可行性评估矩阵
5. 如需异种组合 → 更新异种评估表

---

## 五、回滚操作

### 5.1 Git 回滚（推荐）

```bash
# 撤销最近一次提交
git revert HEAD
git push
# EdgeOne 自动恢复

# 撤销最近 N 次提交
git revert HEAD~N..HEAD
git push
```

### 5.2 Vercel 回滚

```bash
npx vercel rollback
```

### 5.3 紧急处理

如果线上版本出现严重问题：

```bash
# 回退到上一个版本
git log --oneline -10    # 查看最近提交
git revert <commit-hash>  # 回退到指定版本
git push
```

---

## 六、上线前检查清单

| 检查项 | 确认 |
|--------|------|
| 所有参数数据无 Placeholder 占位符 | ⬜ |
| 9 步交互流程完整可走通 | ⬜ |
| 4 条订阅条件各自验证正常 | ⬜ |
| 支付面板展示正常，QR码加载正确 | ⬜ |
| 订阅后参数全部解锁 | ⬜ |
| 刷新页面后订阅状态保持 | ⬜ |
| 移动端适配正常（微信浏览器） | ⬜ |
| Banner 轮播正常 | ⬜ |
| 技术文章弹窗正常 | ⬜ |
| 无 console 报错 | ⬜ |
| SEO meta 标签完整 | ⬜ |

---

## 七、故障排查

### 改了数据但线上没更新

1. 检查 `git push` 是否成功
2. 等待 1-2 分钟（EdgeOne 构建需要时间）
3. 访问 EdgeOne 控制台看构建日志
4. Ctrl+F5 强制刷新浏览器

### 支付后参数仍不显示

1. 检查浏览器控制台是否有报错
2. 确认 localStorage 中 `laserweld_subscription` 数据是否正确
3. 检查 `queryKey`（单次解锁）的指纹是否匹配当前查询参数

### 页面白屏/不加载

1. 按 F12 → Console 看 JS 错误
2. 检查 `index.html` 引用的 JS 文件路径是否正确
3. 如果是 Service Worker 缓存问题，Chrome → Application → Clear storage

### 微信中扫码后无反馈

1. 支付完成后手动点击 **[✅ 我已支付]** 按钮
2. 如仍有问题，引导用户加入微信群人工处理

---

## 八、访问地址

| 平台 | 域名 |
|------|------|
| 🌐 EdgeOne（国内） | https://laserweld-pro-rkmikgiv.edgeone.cool |
| 🚀 Vercel（海外） | https://laserweld-pro.vercel.app |

> ⚠️ `edgeone.cool` 域名可能被微信/QQ拦截，建议尽快绑自定义备案域名

---

## 九、项目文件结构速查

```
激光焊接参数查询工具/
├── index.html              # 主页面（全部核心代码+样式+逻辑）
├── data/
│   ├── params.js           # 焊接参数数据（最常改）
│   ├── articles.js         # 技术文章数据
│   └── sub-factors.js      # 子材料修正系数
├── assets/
│   ├── qr-wechat.png       # 微信收款码
│   ├── qr-alipay.png       # 支付宝收款码
│   ├── icon-192.png        # PWA图标
│   ├── icon-512.png        # PWA图标
│   └── og-preview.png      # 社交分享预览图
├── banners/
│   ├── banner-1.jpg        # 轮播图1
│   ├── banner-2.jpg        # 轮播图2
│   └── banner-3.jpg        # 轮播图3
├── sw.js                   # PWA Service Worker
├── manifest.json           # PWA应用配置
├── favicon.svg             # 浏览器图标
├── maintenance/            # ⭐ 维护更新手册（按需查阅）
├── archive/                # 旧版本备份
├── 需求设计输入文档.md      # 📌 唯一设计输入来源
├── UI设计输入文档.md         # UI视觉规范
├── 软件更新及上线操作指南.md # 本文件
├── DEPLOY.md               # （旧版，被本文件替代）
├── 部署更新指南.md           # （旧版，被本文件替代）
└── 上线全流程开发规划.md     # 开发规划文档（历史参考）
```

---

## 十、快速参考（命令行速记）

```bash
# 日常更新（改参数/改数据）
git add . && git commit -m "更新说明" && git push

# 同步部署到 Vercel（海外用）
npx vercel --prod --yes

# 回滚
git revert HEAD && git push

# 本地测试
python3 -m http.server 8080
```
