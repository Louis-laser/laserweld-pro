# 05 · 更新订阅定价

**目标文件：** `index.html` （搜索 `planOnce` / `planMonthly` / `subDesc` 近处）

## 当前定价方案（2026-06-13更新）

| 方案 | JS变量 | 价格 | 有效期 |
|------|--------|------|--------|
| 单次解锁 | `plan 'once'` | ¥9.9 | 当前查询参数 |
| 月度订阅 | `plan 'monthly'` | ¥38/月 | 30天 |

## 如何修改价格

### 修改展示价格

在 `index.html` 中搜索以下行并修改数字：

```html
<!-- 定价标签（约1432行）-->
<div style="font-size:10px;color:var(--text2);margin-bottom:8px;" data-i18n="sub.gate.price">🔑 单次¥9.9 · 📅 月度¥38/月</div>

<!-- 单次方案价格（约1445行）-->
<div style="font-size:18px;font-weight:700;color:var(--green);margin:2px 0;">¥9.9</div>

<!-- 月度方案价格（约1451行）-->
<div style="font-size:18px;font-weight:700;color:var(--green);margin:2px 0;">¥38<small style="font-size:12px;font-weight:400;">/月</small></div>

<!-- 支付金额说明（约1476行）-->
<div style="font-size:12px; color:var(--text2); margin-bottom:4px;">支付金额（至少¥9.9，多付余额累积）：</div>

<!-- input默认值（约1479行）-->
<input type="number" id="payAmountInput" value="9.9" min="9.9" step="0.1"
```

### 修改JS中的价格常量

搜索以下JavaScript代码并修改数值：

```javascript
// getSubPrice函数（约3027行）
return type === 'monthly' ? '¥38/月' : '¥9.9';

// deductWallet函数（约3070行）
if (w.balance < 9.9) return false;
w.balance -= 9.9;

// confirmPay校验（约3158行）
document.getElementById('payAmountInput').value = '9.9';

// 最低支付校验（约3185行）
if (amount < 9.9) { ... '最低支付¥9.9' ... }

// 自动扣费阈值（约3340行）
if (w.balance >= 9.9) { ... }

// 余额不足提示（约3347行）
(bal < 9.9 ? '（余额不足下次查询）'
```

### 修改国际化文本

```javascript
// 中文本（约1848行）
'sub.gate.price': '🔑 单次¥9.9 · 📅 月度¥38/月',

// 英文本（约2096行）
'sub.gate.price': '🔑 ¥9.9 single · 📅 ¥38/month',
```

### 全部涉及位置速查

| 类型 | 位置（行号） | 说明 |
|------|-------------|------|
| HTML展示 | ~1432 | 订阅门控内价格标签 |
| HTML展示 | ~1445 | planOnce卡片价格 |
| HTML展示 | ~1476 | 支付金额说明 |
| HTML属性 | ~1479 | payAmountInput value/min/step |
| aria-label | ~1442 | planOnce卡片无障碍标签 |
| JS函数 | ~3027 | getSubPrice返回值 |
| JS函数 | ~3070-3071 | deductWallet阈值和扣除值 |
| JS函数 | ~3132 | 扫码提示文案 |
| JS函数 | ~3158 | payAmountInput重置值 |
| JS函数 | ~3185-3186 | 最低支付校验 |
| JS函数 | ~3340 | 自动扣费阈值 |
| JS函数 | ~3347 | 余额不足提示 |
| i18n中文 | ~1848 | 中文价格标签 |
| i18n英文 | ~2096 | 英文价格标签 |

### 修改订阅有效期

```javascript
// index.html 中约 3325-3330 行
// 月度订阅30天有效期
var subAge = Date.now() - sub.savedAt;
if (subAge > 30 * 24 * 60 * 60 * 1000) {
    // 已过期
}
```

### 修改订阅触发条件

搜索 `isHighDifficulty()` 函数（约 2900 行附近），4条条件：

| 条件 | 代码 |
|------|------|
| ≤0.5mm 薄板 | `thickness <= 0.5` |
| 钛金属 | `mat === 'titanium'` |
| 异种材料 | `m1 !== m2` |
| 气密性要求 | 关键词匹配 |

## 修改后同步到EdgeOne

```bash
cd ~/.openclaw/workspace/projects/激光焊接参数查询工具
git add .
git commit -m "定价: 单次解锁¥20→¥9.9 + 材料选择后自动滚动到结果区"
git push
```

EdgeOne自动部署（约1-2分钟），Vercel可选执行：
```bash
npx vercel --prod --yes
```
