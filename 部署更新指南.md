# LaserWeld Pro - 部署更新指南

## 前提

- Vercel 已登录（只需登录一次，后续不需要）
- 如需重新登录：`npx vercel login`

## 更新部署

修改代码后，在项目目录执行：

```bash
cd ~/.openclaw/workspace/projects/激光焊接参数查询工具
npx vercel --prod --yes
```

约10秒完成，线上立即生效。

## 常用操作

### 查看部署状态
```
npx vercel list
```

### 回滚到上一版本
```
npx vercel rollback
```

### 查看部署日志
```
npx vercel logs laserweld-pro.vercel.app
```

## 项目结构

```
激光焊接参数查询工具/
├── index.html          # 主页面（全部核心代码）
├── data/               # 焊接参数数据
├── assets/             # 图片资源
├── sw.js               # PWA离线缓存
├── manifest.json       # PWA配置
├── favicon.svg         # 图标
└── 部署更新指南.md     # 本文件
```

## 注意事项

- 修改 `data/` 下的参数数据后，只需重新部署即可生效
- 修改 `index.html` 后，浏览器可能需要清除缓存才能看到最新版本
- 当前线上地址：https://laserweld-pro.vercel.app
